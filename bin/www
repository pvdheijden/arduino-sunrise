#!/usr/bin/env node

var debug = require('debug')('arduino-sunrise');
var http = require('http');

var app = require('../index.js');

var server = http.createServer(app);
server.listen(process.env.PORT || '8080');